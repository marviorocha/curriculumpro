version: '3'
services:
  db:
    image: nouchka/sqlite3
    volumes:
      - ./tmp/db:/var/lib/sqlite/data
  web:
    build: .
    command: bundle exec rails s -p 3000 -b '0.0.0.0'
    volumes:
      - .:/curriculumpro
    ports:
      - "3000:3000"
   
